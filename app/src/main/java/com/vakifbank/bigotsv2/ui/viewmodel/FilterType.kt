package com.vakifbank.bigotsv2.ui.viewmodel

enum class FilterType {
    ALL,
    ALERTS_ONLY,
    POSITIVE_ONLY,
    NEGATIVE_ONLY
}

enum class SortType {
    DIFFERENCE_DESC,
    DIFFERENCE_ASC,
    NAME_ASC,
    NAME_DESC,
    PRICE_DESC,
    PRICE_ASC
}